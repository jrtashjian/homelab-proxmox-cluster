- name: reboot
  ansible.builtin.reboot:
    msg: "Reboot initiated by Ansible for {{ ansible_hostname }}"
    connect_timeout: 5
    reboot_timeout: 300
    post_reboot_delay: 30
    pre_reboot_delay: 30